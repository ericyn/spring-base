name: spring_base
description: a flutter widget for providing feeling to your app
version: 0.0.1
homepage: https://github.com/ericyn/spring-base

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
